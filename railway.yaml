version: 2
services:
  frontend:
    commands:
      - cd client
      - npm install
      - npm run build
    env: 
      # Variables de entorno para el frontend, si es necesario
  backend:
    commands:
      - cd api
      - npm install
      - npm start
    env: 
      # Variables de entorno para el backend, si es necesario
